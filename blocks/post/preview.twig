{% extends "block-preview-base.twig" %}

{% block preview %}
	<h3>{{ post.get_field('title') | default('This post does not yet have a title ...') }}</h3>
	{{ post.get_field('content') }}
{% endblock %}


